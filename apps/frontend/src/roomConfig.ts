// 방 정원 — 팀 논의로 계속 바뀔 수 있는 값이라 한 곳에서만 관리한다.
// ★ 서버도 같은 검사를 한다 (방 목록 기능) — apps/backend/src/room.ts 의 MIN_PLAYERS·
// MAX_PLAYERS 와 반드시 같은 값이어야 한다. 한쪽만 고치면 "목록에선 들어갈 수 있어
// 보이는데 서버가 거절"하는, 화면에 원인이 안 드러나는 상태가 된다.
// (공유하려고 shared-types 에 뒀다가 되돌렸다 — 그 패키지는 타입 전용이라 런타임
//  값을 넣으면 백엔드 빌드 산출물이 실행 중 죽는다. 그 파일 주석 참고.)
export const MIN_PLAYERS = 4; // 봇 포함. 방장 게임시작 버튼 활성화 기준
export const MAX_PLAYERS = 8; // 봇 포함. 방 정원 고정값 (방장이 개별 설정하지 않음)
